<?php

declare(strict_types=1);

/*
 * SquareConnectAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace SquareConnectAPILib\Models;

use SquareConnectAPILib\ApiHelper;
use stdClass;

class InventoryAdjustmentGroup implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $fromState;

    /**
     * @var string|null
     */
    private $id;

    /**
     * @var string|null
     */
    private $rootAdjustmentId;

    /**
     * @var string|null
     */
    private $toState;

    /**
     * Returns From State.
     * Representative `from_state` for adjustments within the group. For example, for a group adjustment
     * from `IN_STOCK` to `SOLD`,
     * there can be two component adjustments in the group: one from `IN_STOCK`to `COMPOSED` and the other
     * one from `COMPOSED` to `SOLD`.
     * Here, the representative `from_state` for the `InventoryAdjustmentGroup` is `IN_STOCK`.
     */
    public function getFromState(): ?string
    {
        return $this->fromState;
    }

    /**
     * Sets From State.
     * Representative `from_state` for adjustments within the group. For example, for a group adjustment
     * from `IN_STOCK` to `SOLD`,
     * there can be two component adjustments in the group: one from `IN_STOCK`to `COMPOSED` and the other
     * one from `COMPOSED` to `SOLD`.
     * Here, the representative `from_state` for the `InventoryAdjustmentGroup` is `IN_STOCK`.
     *
     * @maps from_state
     */
    public function setFromState(?string $fromState): void
    {
        $this->fromState = $fromState;
    }

    /**
     * Returns Id.
     * A unique ID generated by Square for the
     * `InventoryAdjustmentGroup`.
     */
    public function getId(): ?string
    {
        return $this->id;
    }

    /**
     * Sets Id.
     * A unique ID generated by Square for the
     * `InventoryAdjustmentGroup`.
     *
     * @maps id
     */
    public function setId(?string $id): void
    {
        $this->id = $id;
    }

    /**
     * Returns Root Adjustment Id.
     * The inventory adjustment of the composed variation.
     */
    public function getRootAdjustmentId(): ?string
    {
        return $this->rootAdjustmentId;
    }

    /**
     * Sets Root Adjustment Id.
     * The inventory adjustment of the composed variation.
     *
     * @maps root_adjustment_id
     */
    public function setRootAdjustmentId(?string $rootAdjustmentId): void
    {
        $this->rootAdjustmentId = $rootAdjustmentId;
    }

    /**
     * Returns To State.
     * Representative `to_state` for adjustments within group. For example, for a group adjustment from
     * `IN_STOCK` to `SOLD`,
     * the two component adjustments in the group can be from `IN_STOCK` to `COMPOSED` and from `COMPOSED`
     * to `SOLD`.
     * Here, the representative `to_state` of the `InventoryAdjustmentGroup` is `SOLD`.
     */
    public function getToState(): ?string
    {
        return $this->toState;
    }

    /**
     * Sets To State.
     * Representative `to_state` for adjustments within group. For example, for a group adjustment from
     * `IN_STOCK` to `SOLD`,
     * the two component adjustments in the group can be from `IN_STOCK` to `COMPOSED` and from `COMPOSED`
     * to `SOLD`.
     * Here, the representative `to_state` of the `InventoryAdjustmentGroup` is `SOLD`.
     *
     * @maps to_state
     */
    public function setToState(?string $toState): void
    {
        $this->toState = $toState;
    }

    /**
     * Converts the InventoryAdjustmentGroup object to a human-readable string representation.
     *
     * @return string The string representation of the InventoryAdjustmentGroup object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'InventoryAdjustmentGroup',
            [
                'fromState' => $this->fromState,
                'id' => $this->id,
                'rootAdjustmentId' => $this->rootAdjustmentId,
                'toState' => $this->toState
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->fromState)) {
            $json['from_state']         = $this->fromState;
        }
        if (isset($this->id)) {
            $json['id']                 = $this->id;
        }
        if (isset($this->rootAdjustmentId)) {
            $json['root_adjustment_id'] = $this->rootAdjustmentId;
        }
        if (isset($this->toState)) {
            $json['to_state']           = $this->toState;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
